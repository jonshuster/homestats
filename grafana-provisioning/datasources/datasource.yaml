apiVersion: 1

datasources:
  - name: HomeStats InfluxDB
    type: influxdb
    access: proxy
    database: homestats
    user: ${INFLUXDB_USER}
    password: ${INFLUXDB_USER_PASSWORD}
    url: homestats_database:8086
    isDefault: true
    editable: true